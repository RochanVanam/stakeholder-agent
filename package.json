{
  "name": "gtm-stakeholder-agent",
  "version": "0.1.0",
  "description": "Stakeholder Agent for the GTM Agent Broker hackathon project.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "demo": "node --experimental-strip-types src/index.ts --org example.json --sellerCompany Vercel --targetCompany NVIDIA --productCategory \"frontend hosting/deployment\" --competitor Cloudflare",
    "check:runtime": "node --experimental-strip-types --check src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
